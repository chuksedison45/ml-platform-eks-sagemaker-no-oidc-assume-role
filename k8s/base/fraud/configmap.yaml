apiVersion: v1
kind: ConfigMap
metadata:
  name: fraud-config
  namespace: fraud
data:
  AWS_REGION: "us-west-2"
  SAGEMAKER_ENDPOINT: ""   # set to your real endpoint name, e.g. internal-ml-platform-fraud-endpoint
  SAGEMAKER_TIMEOUT_SECONDS: "5"
  SERVICE_VERSION: "1.0.0"
  LOG_LEVEL: "INFO"
