# TEMPLATE ONLY — DO NOT APPLY THIS FILE IN CI/CD.
# Secrets are created automatically in GitHub Actions (B2) or via scripts/create_k8s_secrets_from_env.sh

apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
  namespace: forecast
type: Opaque
stringData:
  AWS_ACCESS_KEY_ID: "REPLACE"
  AWS_SECRET_ACCESS_KEY: "REPLACE"
  AWS_SESSION_TOKEN: ""   # optional
